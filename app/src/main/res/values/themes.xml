<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TomGuard" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TomGuard" parent="Base.Theme.TomGuard" />

    <style name="ButtonLogout" parent="Base.Theme.TomGuard">
        <item name="android:textSize">16sp</item>
        <item name="android:padding">14dp</item>
        <item name="strokeColor">@color/red_logout</item>
        <item name="strokeWidth">2dp</item>
        <item name="android:textColor">?colorOnPrimary</item>
        <item name="android:backgroundTint">@color/red_logout</item>
    </style>
</resources>